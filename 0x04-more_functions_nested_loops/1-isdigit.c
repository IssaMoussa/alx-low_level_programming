#include "main.h"

/**
 * _isdigit -check
 * @n: input integer
 * Return: 1 if c is digit 0 otherwise
 */

int _isdigit(int n)
{
	char i = '0';
	int is_digit = 0;

	for (; i <= '9'; i++)
	{
		if (i == c)
		{
			is_digit = 1;
			break;
		}
	}

	return (is_digit);
}
